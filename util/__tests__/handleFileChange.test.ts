// import { render, screen } from '@testing-library/react';
// import userEvent from '@testing-library/user-event';
// import UploadForm from '../../app/dashboard/[userName]/UploadForm';

// test('handleFileChange updates selected file count', () => {
//   // Arrange
//   render(<UploadForm />);

//   // Act
//   const fileInput = screen.getByTestId('file-input');
//   expect(screen.getByTestId('selected-file-count').textContent).toBe('0');
//   userEvent.upload(fileInput, new File(['video contents'], 'video.mp4', { type: 'video/mp4' }));

//   // Assert
//   expect(screen.getByTestId('selected-file-count').textContent).toBe('1');
// });

// import { render, screen } from '@testing-library/react';
// import LoginForm from '../../app/(auth)/login/LoginForm';

// describe('LoginForm', () => {
//   it('renders Image component with corresponding values', () => {
//     render(<LoginForm />);

//     // Ensure the Image component is rendered
//     const imageElement = screen.getByAltText('');
//     expect(imageElement).toMatch('Relive your memories');

//     // expect(imageElement).toBeInTheDocument();
//   });
// });
